// Meant to hold all custom styling added to our font and the main font used by our app
// Main variables will be size, weight, family and so on

const Fonts = {
  headerSize: 40,
  subheaderSize: 20,
  regularSize: 16,
  primaryTypo: 'sans-serif-condensed',
  secondaryTypo: 'Roboto',
};

export default Fonts;
