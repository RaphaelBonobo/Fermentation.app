// CurrentFermentContainer

import React from 'react';
import {Text, View} from 'react-native';

const CurrentFermentContainer = (props) => {
  return (
    <View>
      <Text>{'Hello'}</Text>
    </View>
  );
};

export default CurrentFermentContainer;
